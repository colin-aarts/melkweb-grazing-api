{
  "title": "Meta",
  "type": "object",
  "description": "Metadata for registration",
  "properties": {
    "mutableDateMin": {
      "type": "string",
      "description": "Oldest mutable date.",
      "format": "date"
    },
    "mutableDateMax": {
      "type": "string",
      "description": "Newest mutable date.",
      "format": "date"
    },
    "registeredPlots": {
      "type": "array",
      "description": "List of registered plots",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Freeform name"
          }
        },
        "required": [
          "id",
          "name"
        ]
      }
    }
  },
  "required": [
    "mutableDateMin",
    "mutableDateMax",
    "registeredPlots"
  ]
}